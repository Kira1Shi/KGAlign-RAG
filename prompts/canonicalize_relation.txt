Given the following text and a relational triplet extracted from it:

Text: {text}
Triplet: {triple}

The relation '{relation}' is defined as: {relation_definition}

Choose an existing relation that can replace it in this context. The replacement must preserve the meaning, direction from subject to object, and semantic roles of both entities.

Choices:
{choices}

Return only the letter of your choice. Do not return an explanation.
